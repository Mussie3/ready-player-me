import { styled } from '@mui/material/styles';

export const AvatarDisplay = styled('iframe')({
    width: '100%',
    height: '100vh',
    margin: 0,
    padding: '20px',
    fontSize: '14px',
    border: 'none',
})